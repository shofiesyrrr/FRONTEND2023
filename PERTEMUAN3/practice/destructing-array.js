/**
 * membuat array keluarga
 * -suami : sugeng
 * -istri : rusi
 * -anak : shofi
 * kalau array menggunakan []/ kurung siku
 */
const family = ["Sugeng", "Rusi", "Shofi"];

/**
 * melakukan array destructing berdasarkan posisi atau index
 */
const [suami, istri, anak] = family;

//menampilkan nilai array
console.log(suami, istri, anak);